export const metadata = {
  title: "Sanityhuang1988pioneer",
  description: "Appwrite table setup utilities"
};

export default function RootLayout({ children }) {
  return (
    <html lang="zh-Hant">
      <body>{children}</body>
    </html>
  );
}
